version: '3.8'

services:
  backend:
    build:
      context: .
      args:
        - PORT=${PORT:-3001}
    container_name: yotech-backend
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: always
