version: '3.8'

services:
  backend:
    build: .
    container_name: yotech-backend
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: always
